# 关于我 · 沉浸式夜航升级验收

日期：2026-09-24。范围：`/about`，分支 `codex/about-immersive-upgrade`。本轮开发、浏览器验收完成；尚未提交或部署。

上一版验收原文保留在 `artifacts/about-design/2026-09-24/previous-about-night-qa.md`。本报告不改写上一版当时的测试事实；用户后续反馈指出了首屏未越界和全页表现不足，本轮已据此重构。

## 依据与实际内容

- 视觉依据：用户选定图 2 夜航工作台及本轮圈出圆台底座的附件。保留航海世界与全站导航，重点实现完整器物独立跨出海面，并延展全页交互。
- 已用 Playwright 读取并检查线上 `https://www.ranjl.cn/web/about`，确认真实模块包含 7 个项目、11 个技能、3 段工作经历、5 个指标和 4 个兴趣。
- 线上公开资料只通过浏览器路由拦截返回本机页面，未修改数据库、源码默认资料或接口。真实个人信息和截图保留系统临时目录，不纳入仓库。
- 本机浏览器控制连接不可用，用户已明确授权使用本机 Playwright。验证环境为 Windows 本机 Chrome headless，不是跨浏览器或真机跑分。

## 有界视觉检查

第一轮同时检查桌面、手机及项目、生活、联系局部，独立视觉评审未发现内容重叠或裁断，发现并修复下列问题：

1. 手机首屏空白偏长，船台过晚出现：改为文案与阅读提示的自然流，按屏幕宽度为透明前景留出空间。390px 视口首屏高度由 905px 收紧至约 761px，底座能完整进入 844px 高截图。
2. 地图紧凑样式未命中组件实际根类：选择器改为 `.about-map-shell`，布局高度 185px；隐藏地图内重复标题，保留地图商标与版权层。
3. 移动端项目选择后标签可能留在可视区之外：仅滚动标签容器，键盘聚焦使用 preventScroll；横纵排列同步 aria-orientation。
4. 纸面档案的日期与页脚辅助文字对比偏弱：加深颜色；320px 明信片宽度限定为可用宽度。

第二轮已实际打开最终桌面全页、手机首屏/全页、项目档案与生活局部，确认修复结果。桌面实体底座跨过海面边界，章节导航保留净空；手机完整展示器物，后续正文无截断。停止进一步装饰性抛光。

### 最终证据

目录：`C:/Users/Administrator/AppData/Local/Temp/about-upgrade-qa/`。

| 证据 | 尺寸 / 用途 |
| --- | --- |
| `live-before-hero.png`、`live-before-full.png` | 线上改造前页面 |
| `final-desktop-hero.png` | 1440 × 1000，首屏跨界、材质与阅读层级 |
| `final-desktop-full.png` | 1440 × 5041，全页布局和内容节奏 |
| `final-mobile-hero.png` | 390 × 844，完整器物与自然内容流 |
| `final-mobile-full.png` | 390 × 6156，手机全部模块 |
| `final-projects.png`、`final-life.png`、`final-contact.png` | 项目选档、生活场景、联系明信片 |
| `final-visual-metrics.json` | 实际视口、页面高度、无横向溢出及空 pageerror 记录 |

截图保留真实内容，未作为发布资源写入 public。页面文本、按钮、项目资料均为 HTML，生成图像仅承担装饰场景。

## 功能、数据与动效检查

`scripts/check_about_page.cjs` 使用合成接口资料，可重复运行，不依赖本地数据库具体内容。通过项：

- 项目 7 项可全部访问；点击、方向键、Home/End、前后翻档与边界循环；当前技术栈与工具箱高亮联动。
- 简历 dialog 打开、ESC 关闭、焦点回到入口、iframe 关闭后卸载；合成简历用于验证容器生命周期，不声称验证所有远程 PDF 服务。
- 联系明信片翻至二维码、返回正面、展开语义状态。
- 滚动中首屏场景进度、海面 transform、经历罗盘 transform 确实变化；项目档案响应鼠标倾斜。
- 首屏离开视口暂停循环；用户暂停/恢复；浏览器模拟 prefers-reduced-motion 后关闭空间位移、内容完全可读。
- 1440px 桌面以及 768、390、320px 长姓名、长项目名、长简介和长邮箱无页面或正文容器横向溢出；选中项目标签保持可见。
- 空资料有空态、无简历隐藏入口；503 错误有重试入口，重试后恢复资料。
- 所有测试上下文无 pageerror 或非预期 console error；有意模拟的 503 HTTP 错误单独排除。

`scripts/check_about_motion.cjs` 检查实际 composable 的单帧合并、可视场景、滚动进度、显现、禁用、无 IntersectionObserver 降级与刷新/卸载竞态，已通过。事件按需触发 requestAnimationFrame，不运行持续空循环。

## 工程与接受边界

- AboutPage 与 useAboutMotion 定向 ESLint 已通过。前台生产构建（vue-tsc + Vite）已通过，未增加依赖。
- 全站既有 GLTFLoader 与 Dashboard 大于 500 kB 的构建提示仍存在；本次没有修改这些模块。
- 新增海面、透明工作台与生活海岸三张 WebP 合计约 620 KiB，非首屏生活图懒加载。图片请求 high，服务报告海面/生活为 low、前景为 medium，未将请求质量误记为实际输出质量。
- 这是透明栅格素材、原生 CSS 和滚动/指针驱动的空间动效，不是实时 WebGL 船体或流体模拟；未进行持续 FPS、真机、跨浏览器或屏幕阅读器完整验收。
- 保持原导航内容与行为、原接口及后台资料模型。线上数据库将继续决定具体姓名、条目数量、文案和链接。
- 本轮结果在本地预览，未发布线上。
