# 关于我 · 夜航工作台验收

日期：2026-09-24。最终结果：通过，无遗留 P0/P1/P2。

原文章大屏验收原文已归档至 `artifacts/about-design/2026-09-24/previous-article-dashboard-qa.md`；本报告只评价 `/about`，不更改大屏原结论。

## 视觉依据与证据

- 用户选择：图 2「夜航工作台」，`artifacts/about-design/2026-09-24/night/night.png`，1536 × 1024 像素。
- 实现：`http://localhost:5173/about`，真实接口资料、夜色、无弹窗、首屏回到 scrollY=0。首屏截图临时暂停场景动效用于稳定比较，交互验收后已恢复开启。
- 截图目录：`C:/Users/Administrator/AppData/Local/Temp/about-night-qa/`。截图含真实资料，留在系统临时目录，不纳入代码仓库。
- 桌面：`desktop-final.png`，1536 × 1024 像素；CSS 视口 1536 × 1024，devicePixelRatio=1。`desktop-full-final.png` 为 1536 × 3703 全页。
- 手机：`mobile-hero-final.png`，390 × 844 像素；CSS 视口 390 × 844，devicePixelRatio=1。`mobile-final.png` 为 390 × 5209 全页，`mobile-menu-final.png` 为菜单展开态。
- 同输入并排比较：`comparison-final.png`，源图与实现各保留 1536 × 1024，无密度缩放，顶部仅增加 40px 比较标签。
- 同尺寸局部证据：`comparison-hero-copy.png`、`comparison-hero-art.png`、`comparison-workbench.png`，分别检查文本换行、月亮及器物裁切、工作面密度与字号。已实际打开组合比较图，并由独立视觉评审复核最终图片。

## 差异与修复历史

第一轮为 FIX：

1. [P2，已修复] 桌面场景从中心裁切导致月亮触碰顶边。将 `.hero-art`（含宽屏规则）调整为 `object-position: center top`。最终首屏与图像局部证据中月亮完整，船体及主要器物保留。
2. [P2，已修复] 首屏较长主张末尾“日志。”形成孤行。添加 `text-wrap: balance`。桌面、手机均形成均衡两行，不截断接口内容。
3. [P2，已修复] 项目说明、技能和经历辅助信息偏小。提升相关字号至 14–15px，技术标签至 12px，保留栏目层级。最终桌面、手机原始尺寸复核可读。

第二轮独立视觉复核结论：SHIP，上述三项全部 PASS，不追加视觉修正。早期 `desktop-before.png` 因滚动位置不为 0 不作为准确首屏对照；最终组合使用已确认 scrollY=0 的截图。

## 五项视觉检查

| 检查面 | 结果与接受差异 |
| --- | --- |
| 字体与排版 | 展示标题沿用既有航海手写字，正文 Noto Sans SC，读数 IBM Plex Mono；与参考的具体手写笔形存在差异，保留项目字体系统。主张与长文自然换行，辅助信息已加大。 |
| 间距与布局 | 保留左文右景、下方项目/工具并列结构；完整页面采用连续记录面。真实数据增加角色说明，首屏高度、两栏比例及模块长度据此自适应。手机改为文案在上、船景在下，再依阅读顺序单列展开。 |
| 颜色与令牌 | 深海 #061a22、工作面 #0d2933、纸色 #f3edda、辅助 #a9bbc0、黄铜 #e2bc74，适配导航/页脚与地图夜色；按钮、链接与键盘焦点保持对比。局部规则写入 DESIGN.md。 |
| 图像质量 | 以参考生成独立无 UI 的航海场景，保持月海、梅利号、罗盘与日志的材质和构图语言；并非精确复制参考的全部纹理或铭文。WebP 约 202 KiB，实际截图无明显压缩破损。HTML 承载所有界面文字与操作。 |
| 文案与内容 | 身份、主张、经历、项目、技能、兴趣、所在地、本站、联系数据继续读取 API。参考图里的演示文案与项目封面未强加到现有模型；没有新增虚构经历、成绩或技能百分比。 |

顶部导航按用户明确要求只改配色；品牌文字、字体、图标、入口、下拉和移动菜单仍使用原组件，故与参考图的导航外观细节不同。其余新增模块依据接口内容延展，源图未提供这些模块的逐像素设计。

## 交互、数据与工程检查

- 简历预览：原生 dialog 打开、关闭按钮自动聚焦、ESC 关闭、焦点回到入口、关闭卸载 iframe，均已浏览器验证。
- 页内跳转：经历锚点正确，顶部保留约 100px 阅读空间；联系入口到达页面底部联系区，操作可见。
- 手机菜单：展开显示既有全部导航，夜色对比清晰，关闭恢复页面状态。
- 动效：手动暂停、恢复，以及首屏离开视口后 computed animationPlayState=paused 均通过；场景为栅格图像配合 CSS 视差、缓慢缩放与滚动漂移，不是实时 3D 或流体模拟。
- 桌面 1536 × 1024、手机 390 × 844 无页面横向溢出；压力资料 5 条项目、5 段经历、18 个技术、长姓名/文案也无正文容器溢出。已测无头像、无简历、空数组及城市无坐标提示。
- 空接口资料显示模块空态；API 错误显示重试，不显示原硬编码示例身份。点击重新加载后成功恢复，错误消失、aria-busy=false。测试使用独立 fixture 代理，未修改数据库。
- 地图延迟加载、无配置/无坐标状态与异步卸载竞争已检查；真实页面可显示深蓝地图且保留高德商标及版权。
- `scripts/check_viewport_reveal.cjs` 验证实际 composable 的卸载、重叠观察、旧回调、正常显现与减少动态路径，通过。
- 最终修改文件定向 ESLint 无错误和警告；`npm run build`（vue-tsc + Vite）通过。浏览器最终检查未捕获 error/warn。
- 两个 fixture 服务已停止，正式 5173/8000 开发服务保留；响应式 viewport override 已恢复默认。

## 限制与后续事项

- 减少动态偏好和页面隐藏暂停具备代码及相关逻辑检查；未在本轮浏览器中模拟操作系统减少动态设置，不将其记为完整浏览器模拟覆盖。
- 无跨浏览器/真机性能跑分。已有 GLTFLoader、DashboardPage 分包超过 Vite 500 kB 提示，与本轮关于页无关；本轮未全站分包优化。
- Impeccable detector 已执行一次，返回色板/字号与原全站规则差异的 advisory；已补充用户确认的 `/about` 局部系统，不宣称 detector 全绿，也未为消除提示修改其他页面。
- 预览为本地开发结果，未发布线上。线上继续使用原有接口与配置，内容不同将按模块自适应。

## 实施清单

- [x] 三项视觉问题修复并进行有界第二次复核。
- [x] 简历、锚点、菜单、动效及数据边界验收。
- [x] 定向 lint、生产构建、观察器回归检查。
- [x] 更新设计规则与实施记录，保留原验收报告。

final result: passed
