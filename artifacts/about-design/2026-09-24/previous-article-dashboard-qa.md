# 文章大屏验收记录

功能与五条浏览器标注已验证；已有 GLB 与生成稿的船体造型仍有差异，严格视觉还原的 `final result: blocked`。

## 设计与证据

- 确认稿：`artifacts/blog-dashboard/article-focus/文章大屏-设计稿.png`，1672 × 941。
- 最终大屏：`artifacts/blog-dashboard/qa/comments-wide.png`，1922 × 912，示意数据、默认视角。
- 后台设置已通过真实浏览器保存与回读验证；含本地站点配置的后台截图不保留到代码仓库。
- 船体局部对照：`artifacts/blog-dashboard/qa/comparison-model.png`，左侧确认稿、右侧已有 GLB；该对照早于用户要求删除署名小字。
- 原展厅验收记录：`artifacts/blog-dashboard/qa/previous-gallery-design-qa.md`，本轮未复验展厅。
- 素材来源与模型许可：`web/public/dashboard/asset-manifest.json`。

生成请求、旧设计包、重复 GLB、逐帧渲染图、旧版海面与中间截图已按用户要求清理。

## 已验证行为

- 七组 ECharts 图表、六项指标、年份与数据来源切换、刷新及文章明细可用。柱图打开对应月份的 8 篇文章，搜索缩小到 1 篇；本站模式读取 203 篇文章。
- 固定 1672 × 941 画布整体等比缩放、居中留白。1366 × 768、1672 × 941、1920 × 800、2560 × 1080、390 × 844 均已测量，无页面溢出；小窗口保持完整横向画布。
- 1922 × 912 复核时画布为 1620.47 × 912，左右各留 150.76px。留白使用同一海面素材柔化延伸，聚焦与复位后无画布偏移。
- 模型支持拖拽、缩放、复位与自动旋转。拖拽保留开关状态，开启时松手后继续旋转；连续截图模型区域 RGB 平均差异为 [9.61, 7.83, 10.93]。署名小字已移除，来源许可保留在素材清单。
- 后台“站点设置”维护入口与 1 至 12 个年份。实际保存关闭入口、加入 2023 年并刷新，首页入口消失且大屏新增年份；恢复后入口与原年份恢复。
- 设置沿用 JSON 存储和管理员 Session/CSRF 权限，无数据库结构变更。管理员维护的本地运行配置不纳入本次代码提交。

## 工程检查与限制

- 前后台构建和修改文件定向 ESLint 通过；大屏数据测试 3 项、配置与中文错误响应测试 11 项通过。
- 开发热重载期间曾读取到旧后端响应，后端更新后恢复正常，无持续运行错误。
- 全屏按钮界面状态切换已验证；内嵌浏览器的系统级全屏状态读取不一致，未将操作系统级全屏记为完全验证。
- 未主动注入断网或 WebGL 上下文丢失；实现保留提示、重试与图片回退。
- 字体、间距和配色沿用用户确认的大屏设计。真实数据驱动的树图面积、散点和日历格子与生成稿的示意分布不同。
- [P2] GLB 帆面、船头和船体轮廓与生成稿不同；已调整相机并加入水面接触阴影，尚未达到逐像素一致。
- [P3] 字体细节与接触阴影可继续细化；ECharts、Three.js 分包超过 Vite 的 500 kB 提示阈值。
